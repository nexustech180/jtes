# Supabase (create a project at supabase.com, then fill these in)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Paystack (server-side only — never expose the secret key to the browser, PAY-04/05)
PAYSTACK_SECRET_KEY=
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=

# Transactional email provider (INT-04/05)
EMAIL_FROM_ADDRESS=
EMAIL_PROVIDER_API_KEY=
