import PageStub from "@/components/PageStub";

export default function LoginPage() {
  return (
    <PageStub
      title="Client Login"
      note="Will become the single Supabase Auth sign-in shared by every role (public through Super Admin) once task #4 lands — replacing the separate portal.html/admin.html/learn-admin.html logins."
    />
  );
}
